apiVersion: v1
kind: Service
metadata:
  name: database-service
spec:
  type: NodePort
  ports:
  - name: http
    port: 8080 #ensure this matches the exposed port in Dockerfile
    targetPort: 8080 
    protocol: TCP
  selector:
    app: database-service
